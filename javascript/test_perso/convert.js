/*
var input = {binaire : function binary(nb){
    var nb = prompt("binaire:")
   var test = parseInt(nb, 2)
   return alert(test)
}   
}
*/

function binaire(nb){
    var nb = parseInt(prompt("Entrez un nombre binaire:"), 2)
    //var nb = prompt("Entrez un nombre binaire:")"Enter a Value"
    //var convert = parseInt(nb , 2)
    return alert(nb)
}